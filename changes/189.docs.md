The shipped data manifests no longer record the absolute paths the
packages were built from, and the 13 MB stored 2D solution the baseline
tests compare against moved to the `ci-assets` release, fetched on demand
by `tests/data/baseline_asset.py`.
