Validate the installed package against the staged v2 base, extra-model and 2D databases, including checksum checks and low-energy blends. The v2 release remains a draft during testing.

Make the bitwise-reference CI runtime reproducible with its recorded NumPy/SciPy versions and x86-v3 NumPy dispatch; leave the broader platform matrix native. Record the actual numerical runtime in CI logs.
