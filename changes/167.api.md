Physics-relevant default changes in ``mceq_config``:

- ``loss_stencil_method = "expfit_low_upwind2"`` (was ``"expfit"``): the
  continuous-loss derivative operator now uses the exponentially-fitted
  interior stencil with a monotone second-order upwind low-energy
  boundary layer. This is the validated configuration used for the v2
  paper results; it removes the low-energy "boundary cliff" of the pure
  expfit operator (essential for EM cascades on the 1 MeV grid, and it
  only touches the lowest ``loss_stencil_low_upwind_rows`` energy rows).
- ``average_loss_operator = False`` (was ``True``): loss-operator
  averaging is no longer needed for stability with the upwind stencil
  and biases EM shower maxima (number-Xmax ~+2.7 g/cm^2, Nmax ~5.6%).
  Set ``True`` only to reproduce pre-2026-06 historical numbers.
- ``generic_losses_all_charged = True`` (was ``False``): the muon stopping
  power is now applied to all charged hadrons. Without it sub-4-GeV
  protons and charged hadrons never range out (~2 MeV/g/cm^2 over a
  ~2000 g/cm^2 slant column is ~4 GeV of ionization loss) and pile up
  unphysically at the low-energy end of deep-atmosphere spectra. Muon and
  neutrino yields drop by <0.1% for TeV and higher primaries; the effect
  is confined to the lowest decade of the grid. This default *requires*
  ``loss_stencil_method = "expfit_low_upwind2"`` — with the pure
  ``"expfit"`` operator the added hadronic loss rows excite the
  low-energy boundary cliff and deep-slant solves diverge outright. Set
  ``False`` to restore the v1.x behaviour where only muons (and e+-, see
  ``enable_em_ion``) carried continuous losses.
- ``muon_helicity_dependence`` stays ``True`` for flux calculations.
  **Do not set it to False with current databases**: the alternative
  ``decays/unpolarized`` dataset carries a construction defect (the
  inclusive K± -> mu nu entry was averaged with its 3-body duplicate,
  halving the dominant K_mu2 channel), which suppresses conventional
  nu_mu by up to ~40% at TeV energies. This affects all DBs up to and
  including v150 and both MCEq 1.x and 2.x; a DB rebuild fixing the
  dataset is pending. ``enable_em`` runs force the flag off at
  ``MCEqRun`` construction (with a warning), because the helicity L/R
  rows destabilize the EM system; EM shower-maximum observables are
  unaffected by the dataset defect.
