Make the host-pinned golden sections portable across numerically equivalent Linux hosts. The GH runner fleet's glibc (2.39) moves float64 results by a few ULPs against the golden-generating host (2.34), which flips sha256 digests that admit no tolerance: the 1D operator, species, solve1d, environment and paths sections now gate their float payloads through 1/64-stride element-wise samples, per-species reduction rows and rel-L2 bounds at a 1e-9 libm budget, with the digests demoted to identity labels for the collision invariants. structure refreshed with the new config keys.
