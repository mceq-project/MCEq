Memory leak in the MKL sparse solver. `solv_MKL_sparse` allocated two MKL sparse matrix handles per call but never released them, so every MCEqRun.solve() invocation leaked the matrices.
Fixed by calling `mkl_sparse_destroy` on both handles before returning.
