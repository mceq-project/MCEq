Bound the temporary workspace used to build the 2D secant operator on a cold cache, avoiding allocations of tens of GiB on production Hankel grids.

Limit ETD2 steps from the assembled continuous-loss stencil and 2D secant coupling; use finer default density/step settings. Keep the spatial stencil independent of solver tolerances, refresh cached paths after live settings change, and prevent a minimum step from overriding stiffness limits.

Avoid Accelerate crashes for padded column-major sparse matrix products by using per-column GEMV; retain native row-major SpMM for solver batches. Make numerical/source-inspection tests portable across Windows and Python builds.

Publish database downloads atomically after HTTP, length and default-checksum validation. Concurrent first runs no longer truncate a database while another reader opens it; interrupted downloads preserve the previous file.
