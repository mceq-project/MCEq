Fixed `EarthGeometry.set_h_obs` to keep `r_obs` and `theta_max` consistent after
an altitude change: previously only `h_obs` was stored, leaving derived geometry
quantities stale. Also corrected the long-standing location name typo
`SanGrasso` → `GranSasso` (it's Gran Sasso d'Italia, not wherever San Grasso is);
passing the old name now raises a helpful `ValueError`.
