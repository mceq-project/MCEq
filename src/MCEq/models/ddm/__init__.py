"""DDM spline databases and matrix-generation helpers.

``ddm.py`` holds the spline database and the model class, ``ddm_utils.py`` the
matrix-generation helpers. Import names from the submodules; nothing is
re-exported here, so ``automodapi`` sees exactly the submodule surfaces.
"""
