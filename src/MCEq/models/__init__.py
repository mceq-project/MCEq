"""Data-driven particle-production models: imported by the driver only (C4).

The DDM lives under ``models/ddm``. Channel overrides and production
multipliers are applied by
``MCEq.data.interaction_tables.Interactions``.
"""
