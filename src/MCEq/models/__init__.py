"""Physics plug-ins that modify yields: imported by the driver only (C4).

Plan section 1's ``models/``, landed by §13.2 item 7. The DDM lives under
``models/ddm``; ``charm_models`` is listed in C1's exhaustion ledger until
Phase 7 deletes it (decision D6), and ``mod_pprod`` (the channel-override hook,
data §5 risk 18) joins in a later phase.
"""
