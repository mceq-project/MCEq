name: Test

on:
  schedule:
    - cron: '0 22 * * 0'  # Monday 6am Taiwan time
  workflow_dispatch:
  pull_request:
    paths:
      - src/**
      - tests/**
      - pyproject.toml
      - CMakeLists.txt
      - .github/workflows/test.yaml
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-26, macos-latest, macos-15-intel, macos-26-intel, windows-latest, windows-11-arm]
        python-version: ["3.9","3.10", "3.11", "3.12", "3.13", "3.14"]
      exclude:
        - os: windows-11-arm
          python-version: ["3.9", "3.10"]
      fail-fast: false
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: False
        fetch-depth: 3
    - uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ github.job }}-${{ matrix.os }}-${{ matrix.python-version }}
    - uses: astral-sh/setup-uv@v7
    - uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
        allow-prereleases: true
    - name: Install requirements
      run: uv pip install --system . --group test ${{ (runner.os != 'macOS' && runner.arch != 'ARM64') && 'mkl' || '' }}
    - name: Check MKL version
      if: runner.os != 'macOS'
      run: pip show mkl
    - name: Set DB path
      shell: bash
      run: |
        DB_DIR=$(python -c "import importlib.util; import MCEq.config as c; print(c.data_dir)" | tail -1 | tr -d '\n\r')
        echo "MCEQ_CI_DB=${DB_DIR}/mceq_db_v140reduced_compact.h5" >> $GITHUB_ENV

    - name: Cache test database
      uses: actions/cache@v4
      with:
        path: ${{ env.MCEQ_CI_DB }}
        key: mceq-test-db-v1-${{ runner.os }}

    - name: Download test database if not cached
      shell: bash
      run: |
        if [ ! -f "$MCEQ_CI_DB" ]; then
          curl -L https://github.com/mceq-project/MCEq/releases/download/ci-assets/mceq_db_v140reduced_compact.h5 -o "$MCEQ_CI_DB"
        fi
    - name: Run tests
      run: pytest --cov=MCEq --cov-branch --cov-report=xml -n 3
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        flags: ${{ matrix.os }}
